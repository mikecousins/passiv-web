import styled from '@emotion/styled';

export const RebalanceAlert = styled.div`
  position: absolute;
  top: 47%;
  transform: translateY(-50%);
  left: 5px;
  span {
    width: 12px;
    height: 12px;
    display: block;
    border-radius: 50%;
  }
`;
